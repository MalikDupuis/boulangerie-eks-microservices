apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: malikmalikmalik/order-service:latest
          ports:
            - containerPort: 8081
          env:
          - name: DB_USERNAME
            valueFrom:
              secretKeyRef:
                name: order-db-secret
                key: DB_USERNAME
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: order-db-secret
                key: DB_PASSWORD
